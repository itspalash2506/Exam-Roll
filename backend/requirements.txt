fastapi>=0.115.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pdfplumber>=0.11.0
pypdf>=4.3.1
openpyxl>=3.1.5
groq>=0.9.0
sqlalchemy>=2.0.35
pydantic>=2.11.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.1
aiofiles>=24.1.0
aiosqlite>=0.20.0
websockets>=13.0

# Postgres migration (DECISIONS.md, 2026-09-19): asyncpg is the app's own
# runtime driver; psycopg2-binary is a SYNC driver needed only because
# Alembic's migration runner is fundamentally synchronous and cannot drive
# an async engine directly — see alembic/env.py.
alembic>=1.13.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# Session auth (DECISIONS.md, 2026-09-19): argon2id password hashing.
# Never bcrypt (72-byte silent truncation), never a bare hash.
argon2-cffi>=23.1.0

# Object storage — Cloudflare R2 via its S3-compatible API (DECISIONS.md,
# 2026-09-20). boto3 is sync; calls run through asyncio.to_thread() the same
# way this codebase already handles other blocking I/O (Groq, PDF parsing) —
# no new async library needed. moto mocks the S3 API in-process for tests,
# so the R2 code path has real automated coverage without live credentials.
boto3>=1.34.0
moto[s3]>=5.0.0
